#pragma once

// #if __cplusplus >= 201703L && (__has_include(<filesystem>) || __has_include(<experimental/filesystem>))
//     #define FILESYSTEM_AVAILABLE
// #endif

// #if defined(__linux__) || defined(__linux) || defined(linux) || defined(__gnu_linux__)
//     #define KRATOS_COMPILED_IN_LINUX

// #elif defined(__APPLE__) && defined(__MACH__)
//     #define KRATOS_COMPILED_IN_OSX

// #elif defined(_WIN32)
//     #define KRATOS_COMPILED_IN_WINDOWS
// #endif

// add macros to check for compiler support
// add macros to check in which OS the compilation is done

// TODO check configuration => I think Win by default builds debug! => works but has to be changed in case I do performace evaluations!